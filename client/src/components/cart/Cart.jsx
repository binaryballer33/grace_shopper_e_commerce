import { Stack, Typography } from "@mui/material";

const Cart = () => {
	return (
		<Stack
			sx={{
				width: "100%",
				height: "100vh",
				alignItems: "center",
				justifyContent: "center",
			}}
		>
			<Typography variant="h4">Cart</Typography>
		</Stack>
	);
};

export default Cart;
